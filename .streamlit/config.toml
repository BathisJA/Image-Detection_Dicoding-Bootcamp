[theme]
base = "light"
primaryColor = "#E2574C"
backgroundColor = "#FFF9F0"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#2B2118"

[server]
# The app rejects uploads over 1 MB itself; keep the server limit in sync
# so Streamlit's caption matches what the app actually allows.
maxUploadSize = 1
